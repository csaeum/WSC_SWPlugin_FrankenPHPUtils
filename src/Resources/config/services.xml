<?xml version="1.0" ?>
<container xmlns="http://symfony.com/schema/dic/services"
           xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xsi:schemaLocation="http://symfony.com/schema/dic/services http://symfony.com/schema/dic/services/services-1.0.xsd">

    <services>

        <!-- ============================================================ -->
        <!-- FrankenPHP Service -->
        <!-- ============================================================ -->
        <service id="WSC\WSC_SWPlugin_FrankenPHPUtils\Service\FrankenPHPService">
            <argument type="service" id="Shopware\Core\System\SystemConfig\SystemConfigService"/>
            <argument type="service" id="Symfony\Contracts\HttpClient\HttpClientInterface"/>
            <argument type="service" id="logger"/>
            <argument>%kernel.project_dir%</argument>
        </service>

        <!-- ============================================================ -->
        <!-- Plugin-Lifecycle-Subscriber -->
        <!-- ============================================================ -->
        <service id="WSC\WSC_SWPlugin_FrankenPHPUtils\Subscriber\PluginLifecycleSubscriber">
            <argument type="service" id="WSC\WSC_SWPlugin_FrankenPHPUtils\Service\FrankenPHPService"/>
            <tag name="kernel.event_subscriber"/>
        </service>

        <!-- ============================================================ -->
        <!-- API Controller -->
        <!-- ============================================================ -->
        <service id="WSC\WSC_SWPlugin_FrankenPHPUtils\Controller\Api\FrankenPHPApiController" public="true">
            <argument type="service" id="WSC\WSC_SWPlugin_FrankenPHPUtils\Service\FrankenPHPService"/>
            <call method="setContainer">
                <argument type="service" id="service_container"/>
            </call>
        </service>

    </services>
</container>
